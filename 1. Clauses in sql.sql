USE sql_store;

-- SELECT : specify what Columns we want to retrieve
SELECT *

-- FROM : it specifies Tables we want to query
FROM customers

-- WHERE : filter the result
-- WHERE customer_id = 1

-- ORDER BY : it specifies the column we want to sort the result 
ORDER BY first_name

-- FROM, WHERE and ORDER BY are optional and order of them is matter
